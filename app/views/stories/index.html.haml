%h3 Stories:

= link_to 'New story', new_story_path

.stories
  - @stories.each do |story|
    %h2= link_to h(story.title), story
    = simple_format h(story.description)
    = link_to 'Edit story', edit_story_path(story)
