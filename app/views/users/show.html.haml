%h1 Profile
%p= @user.name
%p= @user.email
%p= image_tag @user.avatar(:small)

= link_to 'Stories', stories_path, :class => 'btn'
= link_to 'Edit', edit_user_path(current_user), :class => 'btn btn-warning'
